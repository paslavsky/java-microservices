apply plugin: 'measure'

task("dry-run") {
    group = 'measure'

    doFirst {
        measure.'number of sample starts' = 1
        measure.'number of test requests' = 3
    }
}

tasks.getByName('dry-run').finalizedBy tasks.getByName('measure-all')