rootProject.name = "java-microservices"

def notASample = ['.gradle', 'gradle', 'out', 'build']
file("./samples").eachDir { sub ->
    if (!(sub.name in notASample) && sub.isDirectory())
        include ":samples:$sub.name"
}
